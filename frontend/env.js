/* ============================================================
 * Haramball — environment configuration
 * ------------------------------------------------------------
 * Edit the two values below with your own Supabase project
 * URL and anon (public) key. Both values are safe to commit
 * to a public repo: the anon key is meant to be public, and
 * Row Level Security on the database protects you.
 * ============================================================ */
window.HARAMBALL_CONFIG = {
  SUPABASE_URL:      'https://YOUR-PROJECT.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-PUBLIC-KEY-HERE'
};
